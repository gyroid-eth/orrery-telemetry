"""Narrow, session-bound MCP proxy surface for Codex App agents."""


def serve() -> None:
    """Start the allowlisted MCP proxy after process-scope P0 verification."""

    raise NotImplementedError("MCP proxy is gated on the in-App P0")
